[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "blurry-osint-agent"
version = "0.1.0"
description = "Autonomous Blurry Image OSINT Tracing Agent Demo"
requires-python = ">=3.9"

[tool.setuptools]
package-dir = {"" = "src"}
